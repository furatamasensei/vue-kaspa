{
  "ignoreCommand": "git diff HEAD^ HEAD --name-only | grep -qE '^playground/' && exit 0 || exit 1",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cross-Origin-Embedder-Policy", "value": "credentialless" },
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" }
      ]
    }
  ]
}
